# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# --- SECURITY: FIREBASE ADMIN KEYS ---
# Ignore all JSON files to be safe (except package/config files)
*.json

# ! --- IMPORTANT EXCEPTIONS --- !
!package.json
!package-lock.json
!tsconfig.json
!next.config.json
!jsconfig.json
!.eslintrc.json
!vercel.json

# Explicitly ignore the specific key file (just in case the wildcard fails)
firebase-service-account.json
service-account.json